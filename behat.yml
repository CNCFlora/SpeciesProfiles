default:
    context:
        parameters:
            base_url: http://test.localhost
    paths:
        bootstrap: tests
    extensions:
        Behat\MinkExtension\Extension:
            goutte: ~
            selenium2: ~
